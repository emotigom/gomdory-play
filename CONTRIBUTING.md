# 기여 안내

1. `main`에서 작업 브랜치를 만듭니다.
2. 한 이슈의 관찰 가능한 변화만 담습니다.
3. `npm ci` 뒤 `npm run check`와 `git diff --check`를 실행합니다.
4. PR 설명에는 변경 범위, 의도적으로 제외한 항목, 검증 결과를 적습니다.

의존성은 npm으로만 관리하며 `package-lock.json`을 함께 갱신합니다. 비밀정보나 개인·프로젝트 식별자는 커밋하지 마세요.
